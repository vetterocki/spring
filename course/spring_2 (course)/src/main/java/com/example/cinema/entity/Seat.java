package com.example.cinema.entity;

import com.sun.istack.NotNull;
import lombok.*;

import javax.persistence.*;
import javax.validation.constraints.Min;
/**
 *
 *
 * Second entity to implenet later
 *
 *
 */
/*
@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
@EqualsAndHashCode
@ToString
@Entity
public class Seat {
    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    private Long id;

    @NotNull
    @Min(1)
    private int row;

    @NotNull
    @Min(1)
    private int number;

    public Seat(int row, int number) {
        this.row = row;
        this.number = number;
    }


} */
